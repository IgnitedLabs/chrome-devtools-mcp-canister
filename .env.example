# CDP_PORT is no longer used — the shared Chrome container publishes no
# host port (Chrome's DevTools protocol only ever binds 127.0.0.1 inside
# its own container; see docker-compose.yaml and docs/mcp-client-config.md).
# Kept only so `env_file: .env` in docker-compose.yaml has a file to read.
